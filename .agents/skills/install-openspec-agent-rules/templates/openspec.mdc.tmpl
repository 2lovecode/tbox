---
description: {{PRODUCT_NAME}} 本地压缩版 — 与 AGENTS.md 同政策（不进 git）
alwaysApply: true
---

# {{PRODUCT_NAME}} / OpenSpec（本地）

完整条文以仓库根目录 **`AGENTS.md`** 为准（应用级、进 git）。本文件仅本机 Cursor 注入，勿提交 `.cursor/`。

## MUST

- 新功能 / 行为变更 / 契约变更：先 OpenSpec（`/opsx-propose` 或 `openspec-propose`）→ 实现 → `/opsx-archive`
- 行为真相只认 `openspec/specs/`
- 动手前：`openspec list` 是否已有相关 active change

## 可豁免（回复里一句话说明）

- bugfix / typo / 文案
- 不改变可观察行为的重构
- 测试补强、lint、无契约变化的小依赖升级

## 禁止

- 跳过 OpenSpec 做大范围功能开发
- 恢复 BMAD（`_bmad`、bmad-*）
- 在 `docs/superpowers/` 另建长期规格替代 OpenSpec

## 高风险

加密 / 网络 / 数据完整性：`superpowers-bridge` + Superpowers TDD / 计划 / 审查。

## 新能力落点

{{NEW_FEATURE_PATHS}}
