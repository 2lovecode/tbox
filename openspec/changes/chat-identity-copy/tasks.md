## 1. 身份偏好

- [x] 1.1 新增 `useChatIdentity`（或等价）与用户头像预设常量；读写 localStorage，默认头像 id / 默认助手名「TBox」；用手动或简单单测验证读写与空名回退
- [x] 1.2 实现 `GeneralSettingsPanel`：头像预设网格点选即存、助手显示名 blur/change 写入；打开设置 → 通用可见控件（非占位文案）

## 2. 统一复制

- [x] 2.1 抽出可复用复制图标按钮（悬停/焦点可见、成功勾选 ~1.5s）；替换 `HomePage` 用户/助手现有复制样式，目视确认交互一致
- [x] 2.2 在 `AssistantTrajectory` 工具展开区为入参、出参各加复制按钮；手动验证可分别复制 JSON/结果文本

## 3. 对话身份栏

- [x] 3.1 `HomePage` 用户消息行增加头像（读当前预设）；切换设置后对话内头像立即更新
- [x] 3.2 `AssistantTrajectory` 助手正文行展示助手显示名（头像旁）；改名后无需重建会话即可见；过程轨不加显示名

## 4. 验收

- [x] 4.1 对照 delta specs 手动过一遍：用户头像、助手名、四类复制、重启后偏好仍在；`vue-tsc --noEmit`（或项目既有前端类型检查）通过
