# Single source of truth for the Rust toolchain, shared by local dev and CI.
# `dtolnay/rust-toolchain@master` in the workflows reads this file, so a
# release always builds with the same compiler. Bump deliberately.
[toolchain]
channel = "1.96.0"
