# `dtolnay/rust-toolchain@master` requires an explicit `toolchain` input
# (it does NOT read this file). Workflows parse `channel` below and pass it.
# Bump deliberately; keep workflow and local `rustup` in sync via this file.
[toolchain]
channel = "1.96.0"
