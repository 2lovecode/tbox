---
tool_id: timestamp.convert
keywords: timestamp, 时间戳, unix, 毫秒, 秒, ISO, 日期, 时间转换
---

# 时间戳转换

使用 `timestamp.convert` 在 Unix 秒/毫秒与 ISO-8601 之间转换。

参数：
- `input` — 数字时间戳或 ISO/常见日期时间字符串
- `unit`（可选）— `seconds`、`millis` 或 `iso`
