// Tauri library entry point
// This file is required for the library target defined in Cargo.toml

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
